#Grunt Tasks
bootstrap:
  - modernizr
  - copy:bootstrap
  - concat:vendor

build:
  - clean:dist
  - uncss
  - processhtml
  - cssmin
  - htmlmin
  - concurrent:dist
  - clean:temp
  - connect:dist

default:
  - bootstrap
  - serve

serve:
  - concurrent:server
  - autoprefixer
  - connect:livereload
  - watch